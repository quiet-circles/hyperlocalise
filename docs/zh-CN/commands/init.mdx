---
title: "初始化"
description: "在您的项目中创建或覆盖 i18n.jsonc 模板。"
---

## 用法

```bash
hyperlocalise init [--force]
```

## 它的作用

- 写入 `i18n.jsonc` 从构建-在模板中
- 如果文件已存在，则操作失败 (除非 `--force`)

## 旗帜

- `--force`: 覆盖现有的 `i18n.jsonc`

## 典型流程

```bash
hyperlocalise init
hyperlocalise run --dry-run
```
