---
title: "phiên bản, cập nhật và hoàn tất"
description: "Kiểm tra phiên bản đã cài đặt, tự cập nhật CLI và tạo các tập lệnh hoàn tất shell."
---

## Phiên bản

```bash
hyperlocalise version
```

Định dạng đầu ra mong đợi:

```text
hyperlocalise: <version>
```

## Cập nhật

Cập nhật lên bản phát hành đã xuất bản mới nhất:

```bash
hyperlocalise update
```

Ghim một thẻ phát hành cụ thể:

```bash
hyperlocalise update v1.2.3
```

## Hoàn tất

Sử dụng bản dựng sẵn-trong lệnh hoàn tất Cobra:

```bash
hyperlocalise completion --help
```

Sau đó tạo và cài đặt các script hoàn tất lệnh cho shell của bạn.
